type: icon
description: Symbolic glyph — decorative, referenced by name from icon-set

fields:
  name:
    type: string
    required: true
    max-length: 40
  size:
    type: enum
    values: [sm, md, lg]

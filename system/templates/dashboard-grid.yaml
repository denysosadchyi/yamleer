type: dashboard-grid
description: Slotted multi-zone layout — header, main, optional footer

mode: slotted
slots:
  header:
    allows: [hero-text, page-header]
    cardinality: one
    required: true
  main:
    allows: [section]
    cardinality: many
    required: true
    min: 1
  footer:
    allows: [cta-bar]
    cardinality: one
    required: false
